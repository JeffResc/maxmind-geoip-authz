apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maxmind-geoip-authz.fullname" . }}-test-connection"
  labels:
    {{- include "maxmind-geoip-authz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maxmind-geoip-authz.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
