name: Helm Template

on:
  pull_request:

jobs:
  helm-template:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Render Helm chart
        run: |
          helm template charts/maxmind-geoip-authz > /dev/null
